[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

# Static site: Coolify serves dist/ via nginx when "Is it a static site?" is checked.
# No start command needed.
